# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`vibeweek3`) is currently empty aside from a placeholder `README.md`. No source code, build tooling, dependency manifests, or tests exist yet.

There are no build, lint, or test commands to document, and no architecture to describe, because none of it has been created.

## Guidance for future instances

- Once real code and configuration exist in this repo, re-run `/init` (or manually update this file) to document actual build/lint/test commands and the high-level architecture.
- Do not assume a framework, language, or structure until it is actually present in the repository — check `README.md` and the working directory contents first.

## Language

- 모든 대화(응답)와 커밋 메시지는 한국어로 작성한다.
